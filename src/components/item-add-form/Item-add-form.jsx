import React from 'react';

const ItemAddForm = () => {
    return (
        <form>
            <input type="text"/>
            <button type={'submit'}>Add task</button>
        </form>
    );
};

export default ItemAddForm;